# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# OS
.DS_Store

# Per-movie workspaces hold all generated media & runtime artifacts
# (input_images/, frames, clips, output, storyboard, logs, state). Each run uses
# projects/<name>/ (or projects/default/), so the whole tree is ignored.
projects/
