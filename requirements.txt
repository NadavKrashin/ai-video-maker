# Dependencies are declared in pyproject.toml (single source of truth).
# This file just installs the project (and its deps) in editable mode, so the
# familiar `pip install -r requirements.txt` keeps working.
-e .
